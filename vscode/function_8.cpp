#include<iostream>
using namespace std;
int main()
{
    int a,b;
    cin>>a>>b;

    swap(a,b);  // Inbuild function
    cout<<a<<" "<<b<<endl;

    float f,g;
    cin>>f>>g;
    swap(f,g);
    cout<<f<<" "<<g;

    //Bina function banaye code direct run ho rha hai that is
    // ki already swap naam ka function computer (ya c++ ke code) ke andar exist karta hai known as inbuild function
    //But yadi capital "S" Swap ka use krenge to code nhi chalega
    // kyunki Swap name se koi function inbulild nahi hota hai
    }